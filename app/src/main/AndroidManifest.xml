<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.elis.DepEat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>  <!-- fa capire quale è la main activity-->
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.RegistrationActivity"
            android:label="@string/register_text"></activity>

        <activity android:name=".ui.activities.LoginActivity"
            android:label="@string/login_text">

        </activity>

        <activity android:name=".ui.activities.ShopActivity"
            android:label="@string/shop_text"></activity>

        <activity android:name=".ui.activities.CheckoutActivity"
            android:label="@string/checkout_text"></activity>
    </application>

</manifest>
